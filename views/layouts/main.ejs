<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title || "News CMS" %></title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">

  <style>
  :root {
    --primary: #2563eb;
    --primary-dark: #1d4ed8;
    --text: #1e293b;
    --muted: #4e545e;
    --bg: #f4f6f8;
  }

  body {
    font-family: 'Inter', sans-serif;
    background-color: var(--bg);
    color: var(--text);
    display: flex;
    flex-direction: column;
    min-height: 100vh;
  }

  /* === Navbar === */
  .navbar {
    background: #fff !important;
    box-shadow: 0 2px 12px rgba(0,0,0,0.05);
    position: sticky;
    top: 0;
    z-index: 1000;
    border-bottom: 1px solid #e5e7eb;
    padding: 0.9rem 0;
  }

  .navbar-brand {
    font-weight: 800;
    font-size: 1.45rem;
    display: flex;
    align-items: center;
    color: transparent !important;
    background: linear-gradient(90deg, #2563eb, #3b82f6);
    -webkit-background-clip: text;
    background-clip: text;
  }

  .navbar-brand i {
    color: #2563eb;
    font-size: 1.3rem;
    margin-right: 8px;
  }

  .nav-link {
    color: #334155 !important;
    font-weight: 500;
    transition: all 0.25s;
    border-radius: 0.4rem;
    padding: 0.5rem 0.9rem !important;
  }

  .nav-link:hover {
    color: #fff !important;
    background: linear-gradient(90deg, #2563eb, #3b82f6);
    box-shadow: 0 3px 10px rgba(37,99,235,0.25);
  }

  /* === Heading & Layout === */
  h1.text-center {
    font-weight: 700;
    color: var(--text);
    letter-spacing: -0.5px;
  }

  main.container {
    margin-top: 2rem;
    margin-bottom: 3rem;
  }

  /* === Filter Form === */
  form.bg-white {
    background: #fff !important;
    border: 1px solid #e5e7eb;
    border-radius: 1rem;
    box-shadow: 0 2px 12px rgba(0,0,0,0.04);
    transition: all 0.3s;
  }
  form.bg-white:hover {
    box-shadow: 0 4px 18px rgba(0,0,0,0.07);
  }

  .form-control, .form-select {
    border-radius: 0.75rem;
    border-color: #e2e8f0;
    transition: all 0.25s;
  }
  .form-control:focus, .form-select:focus {
    border-color: var(--primary);
    box-shadow: 0 0 0 0.15rem rgba(37,99,235,0.25);
  }

  button.btn-primary {
    background: linear-gradient(90deg, #2563eb, #3b82f6);
    border: none;
    font-weight: 600;
    border-radius: 0.75rem;
    transition: all 0.25s;
  }
  button.btn-primary:hover {
    opacity: 0.9;
    transform: translateY(-1px);
  }

  /* === Card === */
  .card {
    border: none;
    border-radius: 1rem;
    overflow: hidden;
    background: #fff;
    box-shadow: 0 2px 12px rgba(0,0,0,0.06);
    transition: transform 0.25s ease, box-shadow 0.25s ease;
  }
  .card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 18px rgba(0,0,0,0.08);
  }
  .card-img-top {
    height: 190px;
    object-fit: cover;
  }
  .card-body {
    padding: 1.2rem;
  }
  .card-title {
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--primary);
  }
  .card-text {
    color: #475569;
    font-size: 0.95rem;
  }
  .card-footer {
    background: #f9fafb;
    border-top: 1px solid #f1f5f9;
    padding: 0.8rem 1rem;
  }

  .btn-outline-primary {
    color: var(--primary);
    border-color: var(--primary);
    border-radius: 0.6rem;
    font-weight: 600;
    transition: all 0.25s;
  }
  .btn-outline-primary:hover {
    background: linear-gradient(90deg, #2563eb, #3b82f6);
    color: #fff;
    border-color: transparent;
  }

  /* === Pagination === */
  .pagination .page-item .page-link {
    border: none;
    color: var(--primary);
    font-weight: 500;
    border-radius: 0.6rem;
    margin: 0 3px;
    transition: all 0.25s;
  }
  .pagination .page-item.active .page-link {
    background: linear-gradient(90deg, #2563eb, #3b82f6);
    color: #fff;
    box-shadow: 0 2px 6px rgba(0,0,0,0.1);
  }
  .pagination .page-item .page-link:hover {
    background: rgba(37,99,235,0.1);
  }

  /* === Sidebar === */
  .bg-white.p-3.rounded.shadow-sm {
    border: 1px solid #e5e7eb;
    border-radius: 1rem !important;
    box-shadow: 0 2px 14px rgba(0,0,0,0.05) !important;
    background: #fff;
  }

  .bg-white.p-3.rounded.shadow-sm h4 {
    color: var(--text);
    font-weight: 700;
    margin-bottom: 1rem;
  }

  .bg-white.p-3.rounded.shadow-sm a {
    color: #334155;
    transition: color 0.25s;
    text-decoration: none;
  }
  .bg-white.p-3.rounded.shadow-sm a:hover {
    color: var(--primary) !important;
  }

  /* === Footer === */
  footer {
    background: #fff;
    border-top: 1px solid #e5e7eb;
    padding: 1.2rem 0;
    text-align: center;
    color: var(--muted);
    font-size: 0.9rem;
    margin-top: auto;
  }

  footer i {
    color: var(--primary);
  }

  /* Responsiveness */
  @media (max-width: 768px) {
    .navbar-brand {
      font-size: 1.2rem;
    }
    .navbar {
      display:none;
        }
  }
  </style>
</head>

<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-light">
    <div class="container">
      <a class="navbar-brand" href="/">
        <i class="fa-solid fa-newspaper"></i> NewsCMS
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto align-items-center">
          <% if(user){ %>
            <li class="nav-item"><a class="nav-link" href="/admin/dashboard"><i class="fa-solid fa-gauge me-1"></i>Dashboard</a></li>
            <li class="nav-item"><a class="nav-link" href="/logout"><i class="fa-solid fa-right-from-bracket me-1"></i>Logout</a></li>
          <% } else { %>
            <li class="nav-item"><a class="nav-link" href="/login"><i class="fa-solid fa-right-to-bracket me-1"></i>Login</a></li>
            <li class="nav-item"><a class="nav-link" href="/register"><i class="fa-solid fa-user-plus me-1"></i>Register</a></li>
          <% } %>
        </ul>
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a href="/" class="nav-link">Home</a></li>
          <li class="nav-item"><a href="/blog" class="nav-link">Blog</a></li>
          <li class="nav-item"><a href="/about" class="nav-link">About</a></li>
        </ul>
      </div>
      
    </div>
  </nav>

  <!-- Dynamic Body -->
  <main class="container">
    <%- body %>
  </main>

  <!-- Footer -->
  <footer>
    <div class="container">
      <p>
        <i class="fa-regular fa-copyright"></i>
        <%= new Date().getFullYear() %> NewsCMS. All rights reserved.
      </p>
    </div>
  </footer>

  <!-- Bootstrap Bundle -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
